---
import type { Props } from '@astrojs/starlight/props'
import Default from '@astrojs/starlight/components/Search.astro'
---

{
  // Filter out dynamic pages
  Astro.props.id !== '.md' && <Default {...Astro.props}><slot/></Default>
}
